# Grafana datasources: Mimir (metrics) and Loki (logs). Provisioned on startup.
apiVersion: 1
datasources:
  - name: Mimir
    type: prometheus
    access: proxy
    url: http://mimir:9009
    isDefault: true
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100

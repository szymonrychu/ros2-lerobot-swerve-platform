# Example config for leader arm (e.g. Server).
# Mount this or set FEETECH_SERVOS_CONFIG to its path.
namespace: leader
joint_names:
  - shoulder_pan
  - shoulder_lift
  - elbow_flex
  - wrist_flex
  - wrist_roll
  - gripper
# device: /dev/ttyUSB0
# baudrate: 115200
